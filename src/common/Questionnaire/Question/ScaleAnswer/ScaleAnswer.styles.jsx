import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  scale: {
    paddingLeft: '15px',
  },
  label: {
    fontSize: '1rem',
    paddingTop: '8px',
    marginRight: '5px',
  },
}));

export default useStyles;
