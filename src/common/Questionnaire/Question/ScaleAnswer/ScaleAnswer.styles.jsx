import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  label: {
    fontSize: '0.7rem',
    paddingTop: '8px',
    marginRight: '5px',
  },
}));

export default useStyles;
