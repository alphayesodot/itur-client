import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    padding: '2% 0',
    marginRight: '2%',
  },
  inputLabel: {
    fontWeight: '600',
  },
}));

export default useStyles;
