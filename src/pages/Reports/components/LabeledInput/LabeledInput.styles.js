import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    padding: '0.8% 0',
    width: '15rem',
  },
  inputLabel: {
    fontWeight: '600',
    color: 'white',
  },
}));

export default useStyles;
