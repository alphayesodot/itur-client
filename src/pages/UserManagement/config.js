import { Role } from '../../services/user.service';

const config = {
  adminRoles: [Role.Mada, Role.Technical, Role.Itur],

  // Id of Unit card of admin users (Mada Itur and Technical)
  superUnitId: '507f191e810c19729de860ea',
};

export default config;
