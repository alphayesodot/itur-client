import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {},
  date: {
    direction: 'rtl',
    padding: '2.5em 3em',
    fontWeight: '700',
  },
}));

export default useStyles;
