import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
  },
  dialogTitle: {
    display: 'flex',
    width: '100%',
  },
}));

export default useStyles;
