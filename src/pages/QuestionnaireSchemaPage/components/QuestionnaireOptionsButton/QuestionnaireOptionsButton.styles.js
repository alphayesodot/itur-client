import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    width: '1rem',
    padding: 'auto 0.1rem',
  },
  img: {
    paddingRight: '0.5rem',
  },
  optionIcon: {
    height: '1rem',
  },
}));

export default useStyles;
